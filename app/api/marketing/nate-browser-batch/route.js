import{createHash,timingSafeEqual}from'node:crypto';import{WebSocket}from'undici';
export const runtime='nodejs';export const dynamic='force-dynamic';export const maxDuration=300;
const HASH='31d84302c7ac43c2b5a18bf2dab07b197fef818a943d30f2896a9f9e88aef88d',EXPIRES=Date.parse('2026-09-12T00:00:00Z'),STORE='https://plcqypajqvtpnjctdlho.supabase.co/functions/v1/nate-archive-store',PROFILE='https://site.twstalker.com/Nate_Google_',PID='1544391592106201091',ZONE='nate_archive_browser',SOURCE_JOB='nate-archive-407e3e82e69f4e56144efddf66d6b35f';const H={'cache-control':'private, no-store','x-robots-tag':'noindex','referrer-policy':'no-referrer'},hash=x=>createHash('sha256').update(String(x)).digest('hex');
function txt(v){return String(v||'').replace(/<br\s*\/?\s*>/gi,'\n').replace(/<[^>]*>/g,' ').replace(/&amp;/g,'&').replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/&quot;/g,'"').replace(/&#39;/g,"'").replace(/\u00a0/g,' ').replace(/[ \t]+/g,' ').replace(/\n\s+/g,'\n').trim()}function xid(v){const s=String(v??'');return/^\d{8,24}$/.test(s)?s:null}function arts(v){return[...new Set((JSON.stringify(v).match(/https?:\/\/(?:www\.)?(?:x|twitter)\.com\/(?:i\/article|[^/\s"\\]+\/article)\/\d+/gi)||[]))]}
function norm(body,page){const es=body?.tweets&&typeof body.tweets==='object'?Object.entries(body.tweets):[],rows=[];for(const[k,t]of es){const id=xid(k)||xid(t?.id_str)||xid(t?.id);if(!id||String(t?.core?.screen_name||'').toLowerCase()!=='nate_google_')continue;const d=t.created_at,iso=d&&Number.isFinite(Date.parse(d))?new Date(d).toISOString():null,conv=xid(t.conversation_id_str)||xid(t.conversation_id),kind=t.is_retweet?'repost':conv&&conv!==id?'reply':t.is_quote_status?'quote_post':'post';rows.push({id,source_job:SOURCE_JOB,author_handle:'Nate_Google_',is_authored_by_target:true,content_kind:kind,text:txt(t.full_text||t.text),url:'https://x.com/Nate_Google_/status/'+id,published_at:iso,parent_post_id:null,conversation_id:conv,article_urls:arts(t),raw:{...t,id_str:id,mirror_page:page,mirror_source:'twstalker_via_bright_data_browser'}})}return{entries:es,rows}}
export async function GET(req){const q=new URL(req.url).searchParams,token=req.headers.get('authorization')?.replace(/^Bearer\s+/i,'')||q.get('ticket')||'',want=Math.max(1,Math.min(Number(q.get('pages')||60),120));if(!token||Date.now()>EXPIRES||!timingSafeEqual(Buffer.from(hash(token),'hex'),Buffer.from(HASH,'hex')))return new Response('Not found',{status:404});const key=process.env.BRIGHT_DATA_API_KEY||'';let password='',ws,pageBuf=[],postBuf=[],processed=0,saved=0,lastPage=null,lastOldest=null,exhausted=false;const safe=e=>{let s=String(e?.message||e);for(const x of[key,password])if(x)s=s.split(x).join('[REDACTED]');return s.slice(0,1600)};
 async function http(url,auth,body){const r=await fetch(url,{method:body===undefined?'GET':'POST',headers:{authorization:'Bearer '+auth,'content-type':'application/json'},...(body===undefined?{}:{body:JSON.stringify(body)}),cache:'no-store',signal:AbortSignal.timeout(50000)}),s=await r.text();if(!r.ok)throw Error('HTTP '+r.status+': '+s.slice(0,700));return s?JSON.parse(s):null}const store=(op,b={})=>http(STORE,token,{op,...b}),bd=path=>http('https://api.brightdata.com'+path,key);async function flush(){if(!pageBuf.length)return;const rows=[...new Map(postBuf.map(r=>[r.id,r])).values()];await store('batch_pages',{pages:pageBuf,rows});saved+=rows.length;pageBuf=[];postBuf=[]}
 try{const jobs=(await store('list')).filter(j=>j.request?.mode==='mirror_cursor'&&j.status==='complete'&&j.diagnostics?.hasMore&&j.diagnostics?.cursor).sort((a,b)=>b.request.page-a.request.page),base=jobs[0];if(!base)throw Error('No completed cursor base');let page=base.request.page+1,cursor=base.diagnostics.cursor;const status=await bd('/status'),creds=await bd('/zone/passwords?zone='+ZONE);password=creds.passwords?.[0]||'';if(!status.customer||!password)throw Error('Browser credentials unavailable');ws=new WebSocket('wss://brd.superproxy.io:9222',{headers:{Authorization:'Basic '+Buffer.from('brd-customer-'+status.customer+'-zone-'+ZONE+':'+password).toString('base64')}});const pending=new Map();let seq=0;ws.addEventListener('message',async e=>{let m;try{m=JSON.parse(typeof e.data==='string'?e.data:await e.data.text())}catch{return}if(m.id&&pending.has(m.id)){const x=pending.get(m.id);pending.delete(m.id);clearTimeout(x.timer);m.error?x.reject(Error(m.error.message)):x.resolve(m.result)}});await new Promise((res,rej)=>{const t=setTimeout(()=>rej(Error('connect_timeout')),40000);ws.addEventListener('open',()=>{clearTimeout(t);res()},{once:true});ws.addEventListener('error',()=>{clearTimeout(t);rej(Error('connect_error'))},{once:true})});const send=(method,params={},sessionId)=>new Promise((resolve,reject)=>{const id=++seq,timer=setTimeout(()=>{pending.delete(id);reject(Error('timeout:'+method))},80000);pending.set(id,{resolve,reject,timer});ws.send(JSON.stringify({id,method,params,...(sessionId?{sessionId}:{})}))});const tar=await send('Target.createTarget',{url:'about:blank'}),att=await send('Target.attachToTarget',{targetId:tar.targetId,flatten:true}),sid=att.sessionId,cmd=(m,x={})=>send(m,x,sid);await cmd('Page.enable');await cmd('Runtime.enable');await cmd('Page.navigate',{url:PROFILE});for(let i=0;i<25;i++){const r=await cmd('Runtime.evaluate',{expression:'Boolean(document.querySelector(".add-nw-event"))',returnByValue:true});if(r.result?.value)break;await new Promise(r=>setTimeout(r,1000))}
  const began=Date.now();for(let n=0;n<want&&page<=500&&Date.now()-began<260000;n++,page++){const currentCursor=cursor,form=new URLSearchParams({page:String(page),cursor:currentCursor,data:PID,action:'profile'}).toString(),expr='(async()=>{const r=await fetch("/service/api",{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/x-www-form-urlencoded; charset=UTF-8","X-Requested-With":"XMLHttpRequest"},body:'+JSON.stringify(form)+'});return {status:r.status,text:await r.text()}})()';const ev=await cmd('Runtime.evaluate',{expression:expr,awaitPromise:true,returnByValue:true});if(ev.exceptionDetails)throw Error('browser_fetch_exception_page_'+page);const rr=ev.result?.value;let body;try{body=JSON.parse(rr?.text||'')}catch{throw Error('non_json_page_'+page)}const{entries,rows}=norm(body,page),next=typeof body?.cursor==='string'?body.cursor:null,changed=Boolean(next&&next!==currentCursor),dates=rows.map(x=>x.published_at).filter(Boolean).sort();if(rr?.status!==200&&rr?.status!==201)throw Error('mirror_http_'+rr?.status+'_page_'+page);if(!entries.length&&!changed){exhausted=true;lastPage=page;break}const config={mode:'mirror_cursor',url:'https://site.twstalker.com/service/api',page,cursor:currentCursor,profile_id:PID},id='nate_'+hash(JSON.stringify(config)).slice(0,32),diag={page,transport:'bright_data_browser_api_batch',http:rr?.status,postCount:entries.length,authored:rows.length,postIds:entries.map(([x])=>x),cursor:next,hasMore:changed,oldest:dates[0]||null,newest:dates.at(-1)||null,ingested_at:new Date().toISOString(),ingested_count:rows.length};pageBuf.push({id,request:config,status:'complete',raw_records:[{response:body,transport:'bright_data_browser_api',url:'https://site.twstalker.com/service/api'}],diagnostics:diag});postBuf.push(...rows);processed++;lastPage=page;if(diag.oldest)lastOldest=diag.oldest;if(pageBuf.length>=10)await flush();if(!changed){exhausted=true;break}cursor=next}await flush();return Response.json({processed,saved,lastPage,lastOldest,exhausted},{headers:H})
 }catch(e){try{await flush()}catch{}return Response.json({error:safe(e),processed,saved,lastPage,lastOldest},{status:502,headers:H})}finally{try{ws?.close()}catch{}}}
